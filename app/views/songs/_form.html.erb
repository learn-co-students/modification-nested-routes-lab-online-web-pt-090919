<%= form_for @song  do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %><br>
    <%= f.hidden_field :artist_id %>
  <%= f.label :artist_name %>
    <%= artist_select(f, params) %>
  <br />	  <br>

  <%= f.submit %>
<% end %>
